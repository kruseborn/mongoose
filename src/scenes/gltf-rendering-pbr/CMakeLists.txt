mg_cc_executable(
    NAME
        gltf-rendering-pbr
    SRCS
	gltf_scene.h
	gltf_scene.cpp
	gltf_rendering.h
	gltf_rendering.cpp
        gltf_main.cpp
    COPTS
        ${CPP_FLAGS}
    DEPS
        glm
        mg-engine
        ${VULKAN_LIB}
        ${PLATFORM_LIB}
    DEPS_DIR
        "$ENV{VULKAN_SDK}/include"	
    DEFS
        GLM_FORCE_DEPTH_ZERO_TO_ONE
        GLM_FORCE_LEFT_HANDED
)
