mg_cc_executable(
    NAME
        volume-rendering
    SRCS
        volume_scene.h
        volume_scene.cpp
        volume_rendering.h
        volume_rendering.cpp
		volume_renderpass.h
		volume_renderpass.cpp
        volume_main.cpp
        volume_utils.h
        volume_utils.cpp
    COPTS
        ${CPP_FLAGS}
    DEPS
        glm
        mg-engine
        ${VULKAN_LIB}
        ${PLATFORM_LIB}
    DEPS_DIR
        "$ENV{VULKAN_SDK}/include"
    DEFS
        GLM_FORCE_DEPTH_ZERO_TO_ONE
        GLM_FORCE_LEFT_HANDED
)
