mg_cc_executable(
    NAME
        octree
    SRCS
        octree_scene.cpp
        octree_scene.h
        octree_main.cpp
        octree_rendering.h
        octree_rendering.cpp
        build_octree.h
        build_octree.cpp
        voxelizer.h
        voxelizer.cpp
        empty_render_pass.h
        empty_render_pass.cpp
    COPTS
        ${CPP_FLAGS}
    DEPS
        glm
        mg-engine
        sdfgen
        ${VULKAN_LIB}
        ${PLATFORM_LIB}
    DEPS_DIR
        "$ENV{VULKAN_SDK}/include"
    DEFS
        GLM_FORCE_DEPTH_ZERO_TO_ONE
        GLM_FORCE_LEFT_HANDED
)