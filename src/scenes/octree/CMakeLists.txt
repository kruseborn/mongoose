mg_cc_executable(
    NAME
        octree
    SRCS
        octree_scene.cpp
        octree_scene.h
        octree_main.cpp
        voxelizer.h
        voxelizer.cpp
        empty_render_pass.h
        empty_render_pass.cpp
        render_sdf.h
        render_sdf.cpp
        chunkedAllocator.h
    COPTS
        ${CPP_FLAGS}
    DEPS
        glm
        mg-engine
        sdfgen
        ${VULKAN_LIB}
        ${PLATFORM_LIB}
    DEPS_DIR
        "$ENV{VULKAN_SDK}/include"
    DEFS
        GLM_FORCE_DEPTH_ZERO_TO_ONE
        GLM_FORCE_LEFT_HANDED
)