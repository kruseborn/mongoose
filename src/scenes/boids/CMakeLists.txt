mg_cc_executable(
    NAME
        boids
    SRCS
        boids.h
	    boids.cpp
	    boids_scene.cpp
        boids_scene.h
        boids_main.cpp
    COPTS
        ${CPP_FLAGS}
    DEPS
        glm
        mg-engine
        ${VULKAN_LIB}
        ${PLATFORM_LIB}
    DEPS_DIR
        "$ENV{VULKAN_SDK}/include"
    DEFS
        GLM_FORCE_DEPTH_ZERO_TO_ONE
        GLM_FORCE_LEFT_HANDED
)
